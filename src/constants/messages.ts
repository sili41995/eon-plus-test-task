const enum Messages {
  unauthorized = 'Please, authenticate in the app',
  notFound = 'The request URL was not found on this server',
  ifAccountExist = 'if you have an account',
  ifAccountAbsent = "if you don't have an account yet",
}

export default Messages;
